# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Database
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0
alembic>=1.12.0
pgvector>=0.3.0

# API
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# Data fetching
yfinance>=1.5.1          # <1.5 pins curl-cffi<0.14, which blocks the curl-cffi CVE fix
requests>=2.31.0
feedparser>=6.0.0

# Analysis
scipy>=1.11.0
scikit-learn>=1.3.0
statsmodels>=0.14.0
xgboost>=2.0.0
lightgbm>=4.0.0

# ML Experiment Tracking
mlflow>=2.14.0

# Orchestration
prefect>=3.7.8          # 3.7.8 clears PYSEC-2026-2955 / -2959 (dev-only fixes landed <3.7)

# AI Agent
ollama>=0.4.0
langgraph>=1.1.0
langsmith>=0.8.18       # <0.8.18 has GHSA-f4xh-w4cj-qxq8 + PYSEC-2026-2582/-2583

# NLP / Sentiment Analysis
transformers>=5.5.0     # CVE-2026-5241
torch>=2.13.0           # PYSEC-2025-194
sentence-transformers>=3.0.0

# Medallion Architecture
# DuckDB is the compute engine for the ENTIRE medallion (Bronze→Silver→Gold),
# driven by dbt. dbt-duckdb ATTACHes Postgres via the postgres extension, reads
# Bronze, and materializes Silver + Gold back into Postgres. Right-sized for
# ~1.5M-row data — one engine, one lineage graph, no JVM.
duckdb>=1.1.0
dbt-duckdb>=1.9.0

# Utilities
python-dotenv>=1.0.0

# Development
pytest>=9.0.3           # PYSEC-2026-1845
httpx>=0.24.0

# ---------------------------------------------------------------------------
# Security floors (transitive)
#
# These are pulled in by the direct deps above, not imported directly. Their
# resolvers allow older, vulnerable releases, so we set explicit minimums to
# stop a fresh `pip install` (e.g. a Docker rebuild) from regressing below a
# known CVE fix. Verified clean by `pip-audit`; revisit when the parents pin
# newer floors themselves.
# ---------------------------------------------------------------------------
cryptography>=48.0.1     # GHSA-537c-gmf6-5ccf, PYSEC-2026-36
starlette>=1.3.1         # PYSEC-2026-161/-248/-249/-2280/-2281 (FastAPI request path)
urllib3>=2.7.0           # PYSEC-2026-141/-142
idna>=3.15               # PYSEC-2026-215
curl-cffi>=0.15.0        # PYSEC-2026-2431 (yfinance HTTP client)
langchain-core>=1.3.3    # PYSEC-2026-2563/-2564
langgraph-checkpoint>=4.1.1  # PYSEC-2026-2573
langgraph-sdk>=0.3.15,<0.4.0  # PYSEC-2026-2575 (langgraph 1.1 requires <0.4)
anthropic>=0.87.0        # PYSEC-2026-2114/-2115
pydantic-settings>=2.14.2    # GHSA-4xgf-cpjx-pc3j
Mako>=1.3.12             # PYSEC-2026-2617 (Alembic template engine)
soupsieve>=2.8.4         # PYSEC-2026-3071/-3072
click>=8.3.3             # PYSEC-2026-2132
setuptools>=83.0.0       # PYSEC-2026-3447 (build-time)
