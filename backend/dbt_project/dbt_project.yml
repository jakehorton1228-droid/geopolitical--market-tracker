name: 'gmip'
version: '1.0.0'
config-version: 2

profile: 'gmip'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  gmip:
    staging:
      +materialized: view
      +schema: public
    gold:
      +materialized: table
      +schema: public
